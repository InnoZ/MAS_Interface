.row
  .col-xs-12
    %h2
      %i.icon-chart-line
      Wann werden welche Verkehrsmittel im Tagesverlauf genutzt?
    %hr
  .col-xs-12
    .chart-legend
      - @scenario_a.available_modes.each do |mode|
        - mode_color = @scenario_a.mode_color(mode)
        .chart-legend-item
          .chart-legend-icon{style: "background: #{mode_color}"}
          = t("mode_names.#{mode}").upcase

- [@scenario_a, @scenario_b].each_with_index do |scenario, index|
  - index == 0 ? ab = 'a' : ab = 'b'
  - if scenario
    .row
      .col-xs-12
        .panel.panel-default
          .panel-heading
            .panel-title{class: ('light-panel-title' if ab == 'b')}
              %i.icon-chart-line
              Tagesgang
              .label.label-info= scenario.year
          .panel-body
            %svg.line-chart{id: 'diurnal-curve-chart-' + ab}

  -# .col-sm-6.box
  -#   %h2.text-center Wegedauer
  -#   .box-chart
  -#     %svg#boxplot-chart
